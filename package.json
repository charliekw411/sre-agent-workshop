{
  "name": "sre-agent-workshop-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "node web/build.mjs",
    "test": "npm run test:web",
    "test:web": "node --test web/test/*.test.js"
  },
  "dependencies": {
    "@azure/msal-browser": "5.23.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
